# 🚀 Быстрый старт с Docker

## Для разработки

```bash
# Запуск одной командой
docker-compose up -d

# Или используйте скрипт
# Windows:
docker-start.bat

# Linux/Mac:
chmod +x docker-start.sh
./docker-start.sh
```

Приложение будет доступно: **http://localhost:5000**

## Для продакшена

```bash
# Создайте .env файл
cp .env.example .env

# Отредактируйте .env с вашими настройками
# nano .env

# Запустите продакшен версию
docker-compose -f docker-compose.prod.yml up -d
```

С nginx: **http://localhost** (порт 80)
Без nginx: **http://localhost:5000**

## 📋 Основные команды

| Команда | Описание |
|---------|----------|
| `docker-compose up -d` | Запустить в фоновом режиме |
| `docker-compose down` | Остановить контейнеры |
| `docker-compose logs -f` | Просмотр логов в реальном времени |
| `docker-compose restart` | Перезапустить контейнеры |
| `docker-compose build --no-cache` | Пересобрать образ |
| `docker ps` | Список запущенных контейнеров |
| `docker exec -it vibecodejam_interview bash` | Войти в контейнер |

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
LLM_BASE_URL=https://llm.t1v.scibox.tech/v1
LLM_MODEL=qwen3-coder-30b-a3b-instruct-fp8
LLM_TOKEN=your_token_here
FLASK_ENV=production
```

### Порты

- **5000** - Flask приложение
- **80** - Nginx (только в prod версии)

## 🐛 Отладка

**Проблема**: Контейнер не запускается
```bash
# Проверьте логи
docker-compose logs

# Проверьте статус
docker ps -a
```

**Проблема**: Порт 5000 занят
```bash
# Измените порт в docker-compose.yml
ports:
  - "8000:5000"  # Теперь доступно на порту 8000
```

**Проблема**: Нужно обновить код
```bash
# Перезапустите контейнер
docker-compose restart

# Или пересоберите
docker-compose up -d --build
```

## 📦 Файлы проекта

- `Dockerfile` - Development версия
- `Dockerfile.production` - Production версия с Gunicorn
- `docker-compose.yml` - Development конфигурация
- `docker-compose.prod.yml` - Production конфигурация
- `.dockerignore` - Исключения при сборке
- `.env.example` - Пример переменных окружения
- `nginx.conf` - Конфигурация Nginx
- `docker-start.sh` - Скрипт запуска для Linux/Mac
- `docker-start.bat` - Скрипт запуска для Windows

## 🎯 Готово!

Теперь ваше приложение работает в Docker контейнере! 🎉
