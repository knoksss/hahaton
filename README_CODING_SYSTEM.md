# 🔥 Система автотестов для собеседований

## 📋 Описание

Добавлена полноценная система автоматического тестирования кода для технических собеседований с использованием ИИ для генерации задач и оценки решений.

## ✨ Возможности

### 🎯 Генерация задач
- **ИИ-генерация задач** через LLM на основе позиции и уровня кандидата
- **Fallback задачи** если LLM недоступна
- **Видимые и скрытые тесты** для защиты от читерства
- **Настройка сложности**: easy, medium, hard
- **Поддержка языков**: Python (JavaScript в разработке)

### 🔒 Безопасное выполнение кода
- **Валидация кода** на запрещенные операции
- **Ограничение времени выполнения** (configurable)
- **Ограничение памяти** (только Unix)
- **Песочница** с ограниченным набором встроенных функций
- **Защита от**: os, sys, subprocess, eval, exec, __import__

### ✅ Автоматическое тестирование
- **Запуск всех тестов** включая скрытые
- **Детальные результаты** по каждому тесту
- **Сравнение выводов** с умной обработкой типов
- **Обработка ошибок** и таймаутов
- **Метрики выполнения**: время, память, количество пройденных тестов

### 📊 Анализ качества кода
- **Цикломатическая сложность**
- **Анализ читаемости**
- **Обнаружение code smells**
- **Проверка именования**
- **Подсчет строк кода**
- **Рекомендации по улучшению**

## 🗂️ Структура файлов

```
hahaton/
├── app.py                    # Flask приложение с новыми endpoints
├── coding_tasks.py           # Генератор задач и модель данных
├── code_runner.py            # Безопасный запуск кода и анализ
├── test_coding_system.py     # Тесты системы
└── templates/
    ├── coding.html           # UI для решения задач
    ├── setup.html            # Обновлен: выбор типа собеседования
    └── chat.html             # Существующий чат-интерфейс
```

## 🚀 API Endpoints

### POST /api/generate_coding_task
Генерация новой задачи по программированию

**Request:**
```json
{
  "session_id": "session_xxx",
  "language": "python"
}
```

**Response:**
```json
{
  "success": true,
  "task": {
    "task_id": "task_xxx",
    "title": "Название задачи",
    "description": "Описание...",
    "difficulty": "medium",
    "test_cases": [...],
    "hidden_test_count": 2,
    "solution_template": "def solution():\n    pass",
    "time_limit": 5,
    "memory_limit": 128,
    "hints": ["подсказка1", "подсказка2"],
    "tags": ["массивы", "алгоритмы"]
  }
}
```

### POST /api/submit_code
Отправка кода на проверку

**Request:**
```json
{
  "session_id": "session_xxx",
  "code": "def solution():\n    return 42",
  "language": "python"
}
```

**Response:**
```json
{
  "success": true,
  "test_results": {
    "passed_tests": 5,
    "total_tests": 5,
    "pass_rate": 100.0,
    "execution_time": 0.001,
    "test_results": [
      {
        "description": "Тест 1",
        "passed": true,
        "input": "[1, 2, 3]",
        "expected": "6",
        "actual": "6"
      }
    ]
  },
  "code_quality": {
    "quality_score": 85,
    "readability_score": 80,
    "complexity": 2,
    "lines_of_code": 10,
    "code_smells": [],
    "suggestions": ["Добавьте комментарии"]
  },
  "all_tests_passed": true
}
```

### POST /api/validate_code
Валидация кода без запуска

**Request:**
```json
{
  "code": "import os\ndef hack():\n    os.system('ls')",
  "language": "python"
}
```

**Response:**
```json
{
  "success": true,
  "valid": false,
  "message": "Запрещено использование 'os'"
}
```

### GET /api/get_coding_task
Получение текущей активной задачи

### GET /api/get_submissions
Получение всех попыток решения задач

## 💻 Использование

### 1. Запуск сервера

```bash
python app.py
```

### 2. Создание сессии собеседования

Перейдите на `/setup` и выберите параметры:
- Позицию (Frontend, Backend, Data Scientist, etc.)
- Уровень (Junior, Middle, Senior, Team Lead)
- Тип собеседования
- Тип компании

После создания сессии выберите:
- **ОК** - Теоретические вопросы (существующий функционал)
- **Отмена** - Задачи по программированию (новый функционал)

### 3. Решение задачи

На странице `/coding`:
1. Выберите язык программирования
2. Нажмите "Сгенерировать задачу"
3. Напишите решение в Monaco Editor
4. Нажмите "Запустить тесты"
5. Просмотрите результаты и метрики качества

## 🔧 Конфигурация

В `app.py`:

```python
class Config:
    LLM_BASE_URL = "https://llm.t1v.scibox.tech/v1"
    LLM_MODEL = "qwen3-32b-awq"
    LLM_TOKEN = "your-token-here"
    
    TEMPERATURE = 0.7
    TOP_P = 0.9
    MAX_TOKENS = 2000
```

## 🧪 Тестирование

Запустите тесты системы:

```bash
python test_coding_system.py
```

Тесты проверяют:
- ✅ Запуск кода и выполнение тестов
- ✅ Безопасность и валидацию
- ✅ Анализ качества кода
- ✅ Генерацию задач
- ✅ Полный рабочий процесс

## 📈 Метрики

### Для кандидата отображаются:
- **Пройдено тестов**: X из Y (%)
- **Качество кода**: 0-100
- **Читаемость**: 0-100
- **Сложность**: цикломатическая сложность
- **Строк кода**: количество значимых строк
- **Время выполнения**: в секундах

### Для оценки решения учитываются:
- Прохождение всех тестов (включая скрытые)
- Качество кода (>70 для принятия)
- Отсутствие критических code smells
- Время выполнения (в пределах лимита)

## 🛡️ Безопасность

### Запрещенные операции:
- Импорт: `os`, `sys`, `subprocess`, `eval`, `exec`
- Доступ к файловой системе: `open`, `file`
- Выполнение кода: `compile`, `execfile`
- Пользовательский ввод: `input`

### Ограничения:
- **Время выполнения**: настраивается для каждой задачи (default: 5с)
- **Память**: настраивается (default: 128MB, только Unix)
- **Рекурсия**: стандартный лимит Python

## 🔮 Будущие улучшения

- [ ] Поддержка JavaScript/Node.js выполнения
- [ ] Docker-контейнеризация для изоляции
- [ ] Расширенные метрики (cyclomatic complexity, maintainability index)
- [ ] Детектор плагиата кода
- [ ] Поддержка других языков (Java, C++, Go)
- [ ] Видеозапись процесса решения
- [ ] Метрики времени на каждый тест
- [ ] Графики и визуализация результатов

## 📝 Примеры использования

### Программное создание задачи:

```python
from coding_tasks import CodingTask, TestCase

task = CodingTask(
    task_id="sum_array",
    title="Сумма массива",
    description="Напишите функцию sum_array(arr)",
    difficulty="easy",
    language="python",
    test_cases=[
        TestCase([1, 2, 3], 6, "Простой тест", False),
        TestCase([0], 0, "Один элемент", True),
    ],
    solution_template="def sum_array(arr):\n    pass",
    time_limit=3,
    memory_limit=64,
    hints=["Используйте sum()"],
    tags=["массивы"]
)
```

### Запуск кода с тестами:

```python
from code_runner import CodeRunner

runner = CodeRunner()

code = """
def sum_array(arr):
    return sum(arr)
"""

result = runner.run_python_code(code, task.get_all_tests(), 5, 128)

print(f"Пройдено: {result.passed_tests}/{result.total_tests}")
print(f"Успех: {result.success}")
```

### Анализ кода:

```python
from code_runner import CodeAnalyzer

analyzer = CodeAnalyzer()
analysis = analyzer.analyze_code(code, 'python')

print(f"Качество: {analysis['quality_score']}/100")
print(f"Сложность: {analysis['complexity']}")
print(f"Проблемы: {analysis['code_smells']}")
```

## 🤝 Интеграция с существующей системой

Система полностью интегрирована с существующим функционалом:
- Использует ту же систему сессий (`InterviewSession`)
- Совместима с LLM-клиентом
- Работает параллельно с теоретическими вопросами
- Результаты сохраняются в единой истории сессии

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли (Flask debug mode)
2. Убедитесь, что LLM API доступен
3. Проверьте, что все зависимости установлены
4. На Windows некоторые функции (memory_limit) недоступны

---

**Система готова к использованию!** 🚀

Все компоненты протестированы и работают корректно.
